# Daily Status — 2026-05-24 (Deep Dive, Pillar 1: Performance)

- **State confirmed:** synced investigation base to `submain@22015c4`; builds green both feature configs; tests 243/0 (no-feat), 418/0 (jit), matrix 189 PASS/0 FAIL, parity 120 PASS/69 SKIP/0 FAIL — matches documented baseline (the +2/+2 unit-test delta vs `4d612df` is merge #282).
- **Built Pillar-1 deliverables on `deep-dive-pillar-1`:** 12 benchmark programs (`bench/programs/`), `bench/baseline_0_1.md`, callgrind flamegraphs + text (`bench/flamegraphs/`), `report/performance.md`. Tooling: valgrind callgrind (no `perf` in container) + inferno; `--debug-phase` for per-phase timing.
- **Finding #1 (headline):** every Cx function call zero-allocates a 64 KB arena chunk (`arena.rs:13`, `runtime.rs:127`) → **90% of instructions are `memset`** on call-heavy code. Fix: lazy/non-zeroing arena. Reported, not changed (hot-path redesign needs approval).
- **Finding #2:** variable access uses `HashMap<String,_>`+SipHash → **~26%** in hashing on loop code. Fix: slot-index locals at semantic time (interim: FxHash).
- **Next:** await go-ahead on Pillar 2 (Correctness). **For Zara → backend dev:** JIT perf baselines need (a) phase timers around `prepare_ir`/`execute` in `main.rs`, (b) a short chat on which JIT metrics matter — IR-lowering/codegen are currently uninstrumented.
