# Cx Daily Log — 2026-09-21

## Snapshot

Twenty-second consecutive rest day. No development commits from the project author on any branch in the last 24 hours. The working tree on main is clean. The two commits in the 24-hour window are both automated: yesterday's daily log (`4f53071` on `daily-log-2026-09-20`) and a retroactive blog post for August 7 (`3e05149` on `site`). Submain remains 13 commits ahead of main, unchanged since August 30. The daily-log PR backlog has grown to 25 open PRs. Matrix holds at 414 PASS / 0 FAIL.

## Landed today

Nothing to report. No code, compiler, or language-design commits from the project author in the last 24 hours on any branch.

The two automated commits in the window are documentation-only:

- `4f53071` — daily log 2026-09-20 (on `daily-log-2026-09-20` branch, creating `daily_log/2026-09-20.md`) — CONFIRMED
- `3e05149` — dev log 2026-08-07 (on `site` branch, adding `src/content/blog/2026-08-07.mdx`, 43 lines) — CONFIRMED

Neither contains code changes. The `site` branch entry is a retroactive blog post covering August 7, which was itself a rest day — the post documents the state of submain at that point (26 commits ahead of main, including generic-function monomorphization, call-depth guards, and const/exit lowering) and notes the accumulating merge gap. This continues the pattern of backfilling the public-facing dev blog with posts for dates that had daily logs but no corresponding blog entries.

## In progress / uncommitted work

Working tree on main is clean. No staged changes, no unstaged changes, no untracked files.

**Submain state (unchanged since Aug 30):** 13 commits ahead of main covering array returns via caller-allocated slot, expression receivers for operator dispatch, array and struct assignment copies, Model B (aggregates are values), Model A (contiguous multidimensional arrays), unified assignment-target grammar, `.copy` ABI work (Slices 1 and 2), and a roadmap re-freeze. The continuation blocker remains `copy_into`, which needs `SemanticType::Container` to `IrType` mapping before it can lower.

**Other unmerged branches:** `stokowski/CX-233` (while-in loop lowering), `train/master-backend-cleanup`, `train/backend-docs-reconcile`, `train/backend-while-in`, `train/backend-print-intrinsics`, and the four `deep-dive-pillar-*` branches all remain static. None have received new commits.

## Decisions and direction

Nothing to report. No design decisions or technical direction changes were made today. The site branch's blog post for August 7 is a retroactive content piece documenting a historical rest day, not a new decision.

## Roadmap updates

No items checked off — no development work landed. No new tasks added. ROADMAP.md is unchanged; its content accurately reflects the current state of the project, last updated 2026-08-16 when 0.3.3 shipped.

The daily-log PR backlog now stands at 25 open PRs: #347 (README version update from July 20) plus #392–#415 (daily logs from Aug 27 through Sep 20). None have merged since the backlog migration (#391) landed on main.

## What's next

**Yesterday's predictions vs. today:** All four predicted next steps from the Sep 20 log carried forward unchanged — none were acted on. This marks the twenty-second consecutive day without development activity:

1. `copy_into` / Container lowering — still the direct continuation of the Aug 30 `.copy` work
2. Merge submain to main — still 13 commits ahead, forward merge now 36 days overdue on its oldest commit
3. Nested function definitions — still blocking several fixtures (`t10`–`t13`, `t50_nested_func_no_leak`)
4. Daily-log PR cleanup — backlog grew from 24 to 25 open PRs

The most likely resumption points remain:

1. **`copy_into` / Container lowering.** The blocked piece of `.copy` blocker #3. Needs `SemanticType::Container` to `IrType` mapping.
2. **Merge submain to main.** 13 commits ahead, oldest dating to Aug 16 (36 days). The back-merge resolved containment; forward merge is the straightforward remaining step.
3. **Nested function definitions.** JIT exit 127 on several fixtures.
4. **Daily-log PR cleanup.** Twenty-five open PRs (#347, #392–#415). Merging them would clear the documentation backlog.

The project remains paused at a well-defined boundary. When work picks back up, the container lowering for `.copy` is the clear starting point.

---

**Matrix (main):** 414 PASS / 0 FAIL out of 414 total (unchanged from Sep 20)

**Evidence sources:** `git log --all --since="24 hours ago"` (2 commits, both automated), `git log origin/main..origin/submain` (13 commits, unchanged since Aug 30), `git status` (clean), `git diff` (empty), `find src/ -newer docment/ROADMAP.md` (no new modifications in last 24h beyond those already on main), `daily_log/2026-09-20.md` (from daily-log-2026-09-20 branch), `docment/ROADMAP.md` (read, no changes needed), GitHub PR listing (25 open PRs: #347, #392–#415), `run_matrix.sh` (414/414).
