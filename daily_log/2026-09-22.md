# Cx Daily Log — 2026-09-22

## Snapshot

Twenty-third consecutive rest day. No development commits from the project author on any branch in the last 24 hours. The working tree on main is clean. The one automated commit in the window is a retroactive blog post for July 31 on the `site` branch (`4a0c9cd`). Submain remains 13 commits ahead of main, unchanged since August 30. The daily-log PR backlog has grown to 26 open PRs. Matrix holds at 414 PASS / 0 FAIL.

## Landed today

Nothing to report. No code, compiler, or language-design commits from the project author in the last 24 hours on any branch.

The automated commits in the 48-hour window are documentation-only:

- `4a0c9cd` — "dev log 2026-07-31" on the `site` branch, adding `src/content/blog/2026-07-31.mdx` (39 lines). A retroactive blog post documenting July 31, which was itself a rest day. The post describes submain sitting at 14 commits ahead of main at that time (exit/const lowering, enforcement audit, harness rejection-shape tracking) and the matrix at 380/0. This continues the backfill pattern of generating public-facing blog entries for dates that had daily logs but no blog posts. — CONFIRMED
- `ddc9393` — "daily log 2026-09-21" on `daily-log-2026-09-21` branch, the automated daily log from yesterday. — CONFIRMED

Neither contains code changes.

## In progress / uncommitted work

Working tree on main is clean. No staged changes, no unstaged changes, no untracked files.

**Submain state (unchanged since Aug 30):** 13 commits ahead of main. The work covers array returns via caller-allocated slot, expression receivers for operator dispatch, array and struct assignment copies, Model B (aggregates are values), Model A (contiguous multidimensional arrays), unified assignment-target grammar, `.copy` ABI work (Slices 1 and 2), and a roadmap re-freeze. The continuation blocker remains `copy_into`, which needs `SemanticType::Container` to `IrType` mapping before it can lower.

**Other unmerged branches:** `stokowski/CX-233` (while-in loop lowering), `train/master-backend-cleanup`, `train/backend-docs-reconcile`, `train/backend-while-in`, `train/backend-print-intrinsics`, and the four `deep-dive-pillar-*` branches all remain static. None have received new commits.

## Decisions and direction

Nothing to report. No design decisions or technical direction changes were made today. The site branch's blog post for July 31 is a retroactive content piece documenting a historical state, not a new decision.

## Roadmap updates

No items checked off — no development work landed. No new tasks added. ROADMAP.md is unchanged; its content accurately reflects the current state of the project, last updated 2026-08-16 when 0.3.3 shipped.

The daily-log PR backlog now stands at 26 open PRs: #347 (README version update from July 20) plus #392–#416 (daily logs from Aug 27 through Sep 21). None have merged since the backlog migration (#391) landed on main.

## What's next

**Yesterday's predictions vs. today:** All four predicted next steps from the Sep 21 log carried forward unchanged — none were acted on. This marks the twenty-third consecutive day without development activity:

1. `copy_into` / Container lowering — still the direct continuation of the Aug 30 `.copy` work
2. Merge submain to main — still 13 commits ahead, forward merge now 37 days overdue on its oldest commit
3. Nested function definitions — still blocking several fixtures (`t10`–`t13`, `t50_nested_func_no_leak`)
4. Daily-log PR cleanup — backlog grew from 25 to 26 open PRs

The most likely resumption points remain:

1. **`copy_into` / Container lowering.** The blocked piece of `.copy` blocker #3. Needs `SemanticType::Container` to `IrType` mapping.
2. **Merge submain to main.** 13 commits ahead, oldest dating to Aug 16 (37 days). The back-merge resolved containment; forward merge is the straightforward remaining step.
3. **Nested function definitions.** JIT exit 127 on several fixtures.
4. **Daily-log PR cleanup.** Twenty-six open PRs (#347, #392–#416). Merging them would clear the documentation backlog.

The project remains paused at a well-defined boundary. When work picks back up, the container lowering for `.copy` is the clear starting point.

---

**Matrix (main):** 414 PASS / 0 FAIL out of 414 total (unchanged from Sep 21)

**Evidence sources:** `git log --all --since="24 hours ago"` (1 commit in the strict 24h window, 2 in 48h, both automated), `git log origin/main..origin/submain` (13 commits, unchanged since Aug 30), `git status` (clean), `git diff` (empty), `find src/ -mtime 0` (no files modified today), `daily_log/2026-09-21.md` (from daily-log-2026-09-21 branch), `docment/ROADMAP.md` (read, no changes needed), GitHub PR listing (26 open PRs: #347, #392–#416), `run_matrix.sh` (414/414).
