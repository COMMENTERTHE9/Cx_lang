# Cx Daily Log — 2026-05-28

## Snapshot

Fourth consecutive quiet day. No developer-authored commits landed on any branch in the last 24 hours. The only activity was automated: yesterday's daily log (`dd71d17` on `daily-log-2026-05-27`) and a retrospective blog post (`59fa8e2` on `site`). Main is unchanged since May 22 (PR #278, Type::Void variant). Submain remains 7 commits ahead with the tracker sprint from May 24–25 still unmerged. The matrix holds at 182/0.

## Landed today

No new work landed anywhere in the last 24 hours. The two commits that appeared in the 24-hour window are both automated:

- **daily log 2026-05-27** (`dd71d17`, `daily-log-2026-05-27`) — CONFIRMED. Automated daily log and roadmap update from yesterday's session. Not merged to main.
- **dev log 2026-04-19** (`59fa8e2`, `site`) — CONFIRMED. Retrospective blog post added to the site branch. The site branch hosts the project website and is intentionally separate from the compiler codebase. Despite its commit date of 2026-05-28, the content is a historical dev log for April 19.

No developer-authored commits (by COMMENTERTHE9 or other human contributors) landed on any branch in the last 24 hours.

## In progress / uncommitted work

Nothing to report. The working tree on main is clean — no modified files, no staged changes, no untracked files. No recently modified files detected in `src/`, `docment/`, or `.claude/worktrees/`.

The primary unfinished business is integration, not new code:

- **Submain** is 7 commits ahead of main, carrying tracker closures #001 (composite literal type-checking), #002/#032 (IndexOutOfBounds diagnostics), #008 (builtin registry), #009 (BindingId-indexed variables — 57% speedup on arithmetic loops), #012 (RuntimeError variant), and the `exit()` builtin. Most recent submain commit: 2026-05-25. No merge conflicts expected (submain merged main via PR #282 on May 24).
- **Daily-log branches**: 8 branches pending merge (`daily-log-2026-05-20` through `daily-log-2026-05-27`). Each carries daily log files and incremental roadmap corrections that have not reached main. The last daily-log branch merged to main was 2026-05-09.

## Decisions and direction

No new technical decisions today. The project remains in a holding pattern between the substantial submain work completed May 24–25 and its integration into main. The growing backlog of unmerged daily-log branches (8 pending) accumulates roadmap corrections that drift further from main's stale state.

The roadmap corrections made in this daily-log commit are factual alignment:
- Phase 11 is now fully checked off — all three remaining sub-items (method call lowering `0ab7e9b`, when lowering `bed71c1`, DotAccess compound forms `2d6804a`) were confirmed on main via commit hashes but had never been marked complete in the roadmap.
- Frontend status updated from "0.1 release candidate" / "117/117 matrix" to "v0.1.0 released" / "182/182 matrix" — the `v0.1.0` tag exists and 182 tests pass.

These are corrections to bring the roadmap in line with reality, not new decisions.

## Roadmap updates

**Checked off (confirmed landed on main, previously unmarked):**
- Phase 11 — Method call actual lowering (`0ab7e9b`: impl-methods method-call lowering with literal-width narrowing)
- Phase 11 — `when` block lowering (`bed71c1`: when lowering for Literal/Range/Bool/Catchall + TBool wire-value unknown)
- Phase 11 — DotAccess in compound forms (`2d6804a`: Structs Phase 1+2, impl blocks, compound assign dot-access)
- Phase 11 marked fully complete (all sub-items now checked)

**Corrected:**
- Frontend status: "0.1 release candidate" / "117/117" → "v0.1.0 released" / "182/182"

**Not changed:**
- Phase 8 Round 2 remains incomplete (str/strref layout, Handle\<T\> still pending)
- Phases 12, 14, 15 status unchanged — these items are on submain, not yet on main
- No new roadmap tasks added — no evidence from today's activity to justify new items

## What's next

**Yesterday's predictions vs. today:**
1. "Merge submain -> main" — Did not happen. Fourth consecutive daily log predicting this.
2. "Merge daily-log branches to main" — Did not happen. Backlog grew from 7 to 8.
3. "Lazy arena (tracker #010) or JIT bounds checking (tracker #003)" — Did not happen.

None of yesterday's predicted next moves materialized. The pattern of prediction without action is now four days long.

**Likely next moves:**
1. **Merge submain → main.** This remains the single highest-leverage available action. Submain carries 7 commits with a performance refactor, correctness fixes, and structural improvements. No merge conflicts expected. Every daily log since May 6 has predicted this merge.
2. **Merge daily-log branches to main.** Eight branches pending. This daily-log branch's roadmap update supersedes most earlier corrections, so merging just the latest daily-log branch would be sufficient.
3. **Lazy arena (tracker #010) or JIT bounds checking (tracker #003).** These remain the natural follow-ups from the May 24–25 tracker sprint. The arena fix targets the 64KB memset bottleneck; bounds checking would close the `jit_known_unsound` gap.

---

**Matrix (main):** 182 PASS / 0 FAIL — unchanged from yesterday.

**Reverts:** None in the last 24 hours.

**Evidence sources:** `git log --all --since="24 hours ago"` (2 automated commits), `git log` on main/submain (branch divergence analysis), `git status` (clean working tree), `git diff` (no changes), `find -mtime -1` on src/, docment/, .claude/worktrees/ (no recently modified files), `run_matrix.sh` (182/0), `v0.1.0` tag verification, yesterday's daily log from `origin/daily-log-2026-05-27`, `docment/ROADMAP.md` (stale at 2026-05-09 on main, now corrected).
