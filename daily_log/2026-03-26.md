# Cx Daily Log — 2026-03-26

## Snapshot

Multi-file imports went from syntax-only to fully working in a single day. The resolver, semantic merge, and main.rs pipeline wiring all landed on the `submain` branch in a focused sequence of 10 commits. Module alias resolution, `analyze_resolved_program`, and end-to-end two-file import tests (t74, t64) are all passing. An audit pass followed — five semantic and runtime bugs fixed, dead code removed from 8 source files, and docs synced to match the real state of the language. The matrix runner gained output verification via `.expected_output` sidecars, and six new tests brought the submain matrix to 78/78. None of this has merged to main yet — it is all branch-local committed work on `submain`.

## Landed today

All work today landed on the `submain` branch. No merges to main occurred in the last 24 hours.

**Multi-file import resolution pipeline — CONFIRMED** (3 commits: `cb5ae05`, `be19e84`, `098818b`)

This is the day's most significant work, completing the #1 predicted next step from yesterday. Three commits built the full pipeline:

- `cb5ae05` — Wire module alias resolution into `analyze_method_call` in `semantic.rs`. Method calls on imported module aliases now resolve through the module's export table.
- `be19e84` — Add `analyze_resolved_program` in `semantic.rs`. This is the multi-file semantic merge — it takes a `ResolvedProgram` (from the resolver) and produces a unified semantic analysis across all files in topological order.
- `098818b` — Wire `analyze_resolved_program` into `main.rs`. The full pipeline is now: parse → resolve imports → topo-sort → semantic merge → runtime dispatch. Multi-file programs can now actually run.

The resolver module (`src/frontend/resolver.rs`, 301 lines new) provides the dependency graph infrastructure: `ModuleId`, `ResolvedFile`, `ResolvedProgram`, `ImportEdge`, circular import detection, and topological sorting. This module was added in earlier submain commits (pre-24h window: `985a226`, `c711135`, `92a264f`).

**t74 two-file import test — CONFIRMED** (`9eb9060`)

`t74_import_basic.cx` imports `t74_import_basic_lib.cx` via `#![imports]` and calls `math.add(10, 20)` and `math.multiply(3, 4)` across the file boundary. This is the first multi-file program that runs end to end in Cx. The parser also received a small fix in this commit.

**Five semantic and runtime bugs fixed — CONFIRMED** (`3d6112c`)

An audit pass found and fixed five bugs across `parser.rs`, `semantic.rs`, and `runtime.rs` (3 files, +22/-11 lines). Commit message says "five semantic and runtime bugs from audit" — specific bug details are in the diff but the scope is correctness fixes, not new features.

**Dead code deletion — CONFIRMED** (`21b215c`)

Removed `Print`/`PrintInline`, `Range`, `Placeholder`, and `wait_for_step` from 8 source files: `ast.rs`, `diagnostics.rs`, `parser.rs`, `semantic.rs`, `semantic_types.rs`, `lower.rs`, `main.rs`, `runtime.rs`. These were remnants of the old interpreter path and pre-function-print era.

**Documentation accuracy pass — CONFIRMED** (`8eac6fe`)

Six documentation files synced to current language state: `ROADMAP_v3.1.md`, `cx_backend_roadmap_v3_1.md`, `backlog_notes.md`, `cx_flags.md`, `cx_syntax.md`, and `docs/frontend/ROADMAP.md`. The `cx_syntax.md` file received the largest update (+158 lines), likely reflecting all the syntax that has shipped since it was last updated.

**New tests t64, t75, t76 — CONFIRMED** (`00a5429`)

- `t64_import_pub_only.cx` + `t64_import_pub_only_lib.cx` — tests that only `pub` functions cross the import boundary
- `t75_string_interpolation.cx` — verifies `{varname}` expansion in print
- `t76_block_comments.cx` — verifies `/# ... #/` block comment syntax

**Output verification in matrix runner — CONFIRMED** (`286ea5e`)

`run_matrix.sh` now supports `.expected_output` sidecar files. If a test has a corresponding `.expected_output` file, the runner compares actual output against expected output and reports `PASS (output verified)` or `FAIL (output mismatch)`. Two sidecar files shipped: `t75` and `t76`.

**Roadmap v4.5 — CONFIRMED** (`3fa255b`)

The submain roadmap was updated to mark two quality gates done: struct field type checking (DotAccess now resolves real field types) and method call return type resolution (method_registry resolves return types). Test matrix count updated to 78 throughout.

**Site branch — CONFIRMED** (`aab3496` on `origin/site`)

Dev log blog post for 2026-03-24 committed to the site branch. Website work, not language implementation.

## In progress / uncommitted work

Working tree is clean. No staged changes, no untracked files, no uncommitted diffs. No recently modified files in `src/`, `.claude/worktrees/`, or `docs/` newer than the roadmap. All today's work is fully committed on `submain`.

The submain branch itself has 13 commits not yet on main. This represents significant accumulated work — the resolver module, the semantic merge, the full multi-file pipeline, bug fixes, dead code removal, new tests, and doc sync. Integration to main is the obvious next step.

## Decisions and direction

**Multi-file imports: from syntax to working pipeline in one day.** Yesterday's log noted that import syntax was parsed but "no actual file loading happens." Today's submain commits complete that gap: the resolver loads files, builds a dependency graph with topological sorting, detects circular imports, and the semantic layer merges across file boundaries. The `analyze_resolved_program` function is the key new abstraction — it walks the topo-sorted file list and builds a unified analysis. This is a hard blocker checked off on submain.

**Pub enforcement is being tested.** The t64 test (`t64_import_pub_only.cx`) specifically tests that only `pub`-marked functions are visible across import boundaries. This suggests the resolver already enforces visibility — not just module loading.

**Output verification is a quality infrastructure investment.** The matrix runner now does more than just check exit codes — it can verify actual program output. This is a step toward the kind of conformance testing that a language release needs.

**Roadmap underwent a structural edit on submain.** The submain version (v4.5) removes some historical Done entries (IO + Generic Structs Sprint, Macro + Import Syntax Sprint) and several Key Changes sections that existed in the main version (v4.6). It also re-added t42 as a known gap (it was marked resolved on main). The version number went down, which appears to be intentional simplification rather than a regression. The main roadmap will need reconciliation when submain merges.

## Roadmap updates

**Context:** The roadmap on `main` (`docs/frontend/ROADMAP.md`) is v4.6. The submain branch has a v4.5 version with different edits. Since the submain work hasn't merged yet, I'm making conservative edits to the main roadmap reflecting what's clearly evidenced on submain.

**Updated in Active section:**
- Multi-file imports updated to note that full resolution pipeline is implemented on submain (resolver, semantic merge, runtime dispatch, t74/t64 passing), pending merge

**Updated in Known Gaps:**
- Struct field type checking and Method call return type — noted as fixed on submain per quality gate evidence

**Not checked off on main:**
- Multi-file imports hard blocker — the work exists on submain and is confirmed by tests, but hasn't merged to main. Leaving it unchecked on main with a note.
- Other hard blockers unchanged.

**No new roadmap items added.** The work today was execution within existing roadmap scope, plus quality improvements (output verification, dead code removal) that don't need their own roadmap entries.

## What's next

1. **Merge submain to main.** 13 commits of real work — resolver, semantic merge, bug fixes, dead code removal, six new tests, doc sync — are sitting on submain. This is the most obvious next step. The multi-file imports hard blocker is functionally done but only on a branch.

2. **Test runner and assert.** Yesterday predicted this as #2, and it remains the next major hard blocker after imports. `#[test]` is parsed, import infrastructure is in place. `assert(cond)`, `assert_eq(a, b)`, and a `cx test` command are the remaining pieces.

3. **Error model (Result\<T\>).** Also a hard blocker. With imports and the test infrastructure converging, the error model is the next large design+implementation piece.

4. **Backend IR Phase 6 continuation.** Predicted for three consecutive days and hasn't landed. Loops and structs in the IR lowering pipeline remain open. This may be intentionally deferred while frontend hard blockers are closing.

5. **Reconcile roadmap versions.** The main (v4.6) and submain (v4.5) roadmaps have diverged structurally. When submain merges, the roadmap will need a single coherent version.

---

**Yesterday's predictions vs. today:**
- "Module resolution" — **CONFIRMED.** This was prediction #1 and it fully landed on submain. The resolver, semantic merge, and main.rs wiring all shipped. t74 and t64 are passing.
- "Test runner and assert" — did not happen. Import work and the audit/cleanup pass took the day.
- "Generic structs Phase 3" — did not happen. Third consecutive day deferred.
- "Backend IR Phase 6 continuation" — did not happen. Third consecutive day deferred.
- "Matrix at 72/72" — now 78/78 on submain (6 new tests), still 72/72 on main (no merge yet).

Yesterday's top prediction was exactly right. The audit, bug-fix, and dead-code-removal work were not predicted but represent the kind of hardening that follows a major feature landing.

**Matrix status:** 72/72 on main (unchanged from yesterday). 78/78 claimed on submain (6 new tests: t64, t74, t75, t76, plus expected_output verification for t75 and t76). Main matrix has not regressed.

**Evidence sources used:** git log across all branches, commit diffs and stats, file contents on submain via git show, run_matrix.sh execution on main, yesterday's daily log, both versions of the roadmap.
