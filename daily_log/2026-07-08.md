# Cx Daily Log — 2026-07-08

## Snapshot

One commit landed on submain in the last 24 hours: `cff4e2d`, a 180-line known-issues tracker (`docs/known_issues.md`) documenting the f64 comparison fix and four open interpreter/JIT divergences found during recent work. No code changes, no merges, no uncommitted work. Matrix holds at 292/0. Submain is now 9 commits ahead of main — fourth consecutive day without the predicted merge.

## Landed today

- **CONFIRMED** — `cff4e2d` on `submain`: created `docs/known_issues.md`, a structured engineering audit document tracking five known issues with code-level precision (file paths, line numbers, mechanism analysis):
  1. **f64 comparison in interpreter** — FIXED in `c5e8e22`. Four missing `Value::Float` match arms in `src/runtime/ops.rs:173-208` for relational operators. Unusual direction: the JIT was already correct and the interpreter (normally the reference) was wrong.
  2. **Bare builtin call in trailing position** — OPEN. A bare `print(...)` as a function body's last statement (no semicolon) fails to JIT-lower with `UnresolvedSemanticArtifact`. The parser promotes it to `ret_expr`, which routes through `lower_expr` instead of `lower_stmt` where builtins are intercepted. Not method-specific — confirmed via free-function reproducer.
  3. **Explicit return + trailing expression** — OPEN, deferred. Functions with both an explicit `return` and a dead trailing expression trigger `InternalInvariantViolation` at `src/ir/lower.rs:677-684`. Affects `t03`, `t160`, `t24`.
  4. **print(enum) divergence** — OPEN. Interpreter prints `Color::Green`, JIT prints the raw tag `1` because enum erases to `I8` at lowering.
  5. **I128 printing not lowered** — OPEN, fully scoped, ready to build. Missing match arm in `route_print_arg` at `src/ir/lower.rs:4646-4667`.

- **Automated** — `a333685` on `daily-log-2026-07-07`: yesterday's daily log (documentation infrastructure, not language work).

## In progress / uncommitted work

Nothing to report. Working tree on main is clean — no staged, unstaged, or untracked files.

The known-issues doc is the only change since yesterday. All other submain work (D2.5 Handle core, when guards/as-v binding, f64 fix, roadmap reconciliation) was committed in earlier sessions and has not changed.

## Decisions and direction

The known-issues document represents a deliberate quality decision: pausing feature work to catalog and scope interpreter/JIT divergences before moving forward. The document is structured as a living log (entries updated, not deleted) which signals an intention to track these systematically rather than fix them ad-hoc.

Two of the five issues (#2 and #3) sit in the same code region — the function trailing-expression / `ret_expr` handling — but the doc explicitly confirms they are distinct bugs with different error variants and different triggers. Issue #5 is explicitly marked "ready to build whenever picked up," which makes it the most likely next fix target if the developer returns to JIT convergence work.

The submain-to-main integration gap continues to grow. Submain is now 9 commits ahead (up from 8 yesterday), with three complete feature scopes (D2.5 scalar Handle, pattern matching guards/binding, interpreter float fix) plus the roadmap reconciliation and now this known-issues tracker. The merge has been predicted for four consecutive daily logs and has not happened.

## Roadmap updates

No items were checked off on the main roadmap. The one commit today was documentation on submain, not a code milestone landing on main.

A working note was added to `docment/ROADMAP.md` recording the current state: submain at 9 commits ahead, known-issues tracker created, matrix stable at 292/0.

The main roadmap remains significantly stale (last updated 2026-05-18). The authoritative version lives on submain (updated 2026-07-04, commit `10ac2ed`) with the corrected version sequence (0.3.1 through 1.0+) and shipped-version structure. This divergence resolves when submain merges to main.

## What's next

**Yesterday's predictions vs. reality:**
- "Merge submain -> main" — Did NOT happen (fourth consecutive miss)
- "Tag 0.3.1 and/or 0.3.2" — Did NOT happen
- "New feature work on submain" — Partially: the known-issues doc is project work, but not feature work

**Likely next moves:**
1. **Merge submain -> main.** Still the single most impactful integration step. 9 commits, three feature scopes, a roadmap reconciliation, and a known-issues tracker are queued. The repeated prediction-miss pattern suggests this may be blocked on something not visible in the repo (review cadence, confidence gate, external constraint) rather than simply deferred.
2. **Fix known issue #5 (I128 printing).** Explicitly marked "ready to build" in the known-issues doc. One new match arm in `route_print_arg` plus one host callback — the smallest-scoped open issue.
3. **Phase 11 remaining: `when` block lowering or DotAccess in compound forms.** The pattern matching work on submain (guards, as-v binding) may partially address the `when` lowering item, but the roadmap item on main still shows it as open.

---

**Matrix:** 292 PASS / 0 FAIL (unchanged from yesterday).
**No reverts detected.**
**No merge commits in the 24-hour window.**
**No uncommitted work in the current working tree.**

**Evidence sources used:**
- `git log --all --since="24 hours ago"` — 2 commits (1 real + 1 automated)
- `git log origin/main..origin/submain` — 9 branch-local commits confirmed
- `git show cff4e2d -- docs/known_issues.md` — full diff of the day's commit read and analyzed
- `git status`, `git diff`, `git diff --cached` — clean working tree confirmed
- `git show origin/daily-log-2026-07-07:daily_log/2026-07-07.md` — yesterday's log for continuity
- `git show origin/submain:docment/ROADMAP.md` — authoritative roadmap state (2026-07-04)
- `docment/ROADMAP.md` on main — stale roadmap (2026-05-18) for comparison
- `run_matrix.sh` — 292/0 confirmed
- `find` over `src/`, `docment/`, `.claude/worktrees/` — no meaningful modification-time signals beyond filesystem timestamps from fresh clone
