# Cx Daily Log — 2026-07-25

## Snapshot

Documentation site sprint. After v0.3.2 shipped yesterday and the submain merge landed, today's work shifted entirely to the doc site (`site` branch) — catching it up with two releases' worth of new language features. Seven commits in a focused 30-minute burst added the first-ever gene/phen documentation page (145 lines), expanded control-flow docs with pattern binding and guard clauses, refreshed all JIT parity numbers and gap lists to match v0.3.2 state, bumped version claims from 0.3.0 to 0.3.2, replaced the favicon, and fixed an MDX rendering issue. All work is branch-local on `site`, not yet merged to main. Matrix holds steady at 380/0.

## Landed today

All seven commits landed on the `site` branch between 20:28 and 20:58 EDT on Jul 24. The `site` branch is the Astro-based documentation website and is separate from the main language implementation. None of these commits are on `main` — they are branch-local work.

- **CONFIRMED** — `8804078`: New `gene-phen.mdx` page, 145 insertions. This is the first documentation of the trait system anywhere on the site. Covers gene contracts, phen expressions, cross-type dispatch, generic bounds (`T: GeneName`), operator overloading via prelude genes, and the interim expression-receiver restriction. All code samples are verbatim excerpts from real test fixtures (`t_gene_phen_call_basic`, `t_gene_phen_call_dispatch`, `t_gene_bound_dispatch`, `t_op_gene_add`, `t_op_gene_ord`), each verified against both backends at submain HEAD.

- **CONFIRMED** — `c8479ba`: Added pattern binding (`as v`) and guard clauses to `control-flow.mdx`, 51 insertions. These shipped in v0.3.1 with zero site coverage. Three samples from real fixtures, all verified on both backends.

- **CONFIRMED** — `dcac7fc`: Refreshed JIT gap lists across `backend-status.mdx`, `generics.mdx`, `introduction.mdx`, `structs-and-methods.mdx`, and `index.astro`. Key update: the print-in-method-body caveat (added Jul 5) is retired — print in methods now lowers correctly. Added gene/phen and pattern binding rows to the backend-status table, and a gene/phen feature card to the homepage. 5 files, 16 insertions, 7 deletions.

- **CONFIRMED** — `dba484a`: Updated parity numbers sitewide from stale 308/248/60/0 to the live-verified 380/319/61/0 in `introduction.mdx`, `backend-status.mdx`, and `index.astro`. Also dropped the "Phase 15 active" label — the phase-numbered backend roadmap is now historical.

- **CONFIRMED** — `5ecbd69`: Bumped version claims from v0.3.0 to v0.3.2 in `ExperimentalBanner.astro`, `introduction.mdx`, and `site.ts`. The site hadn't been synced since 0.3.0.

- **CONFIRMED** — `2357edc`: Replaced the stale grayscale favicon with the CX mark. Binary change: 655 bytes to 2696 bytes.

- **CONFIRMED** — `82466b5`: Escaped bare `Handle<T>` in the 2026-07-24 blog post MDX to fix a rendering issue where the angle brackets were parsed as JSX.

**Combined stats for today's work:** 10 files changed, 220 insertions, 15 deletions.

## In progress / uncommitted work

Nothing to report. Working tree is clean on both `main` and `site`. All documentation site work has been committed.

## Decisions and direction

1. **Documentation site is catching up after two releases.** The site was stuck at v0.3.0 claims. Today's sprint brought it current with v0.3.2. The gene/phen page, pattern binding docs, and refreshed gap lists represent the first site coverage of features that shipped across 0.3.1 and 0.3.2.

2. **All code samples are verified against both backends before inclusion.** The commit messages for `gene-phen.mdx` and `control-flow.mdx` explicitly document that samples were run under `--backend=interp` and `--backend=cranelift` at submain HEAD (54ab720). This is a documentation-as-test discipline: no sample is invented.

3. **Print-in-method caveat retired.** A JIT limitation documented since Jul 5 (`structs-and-methods.mdx`) no longer holds — the gap closed somewhere in the v0.3.2 work. The commit verified this empirically rather than assuming it from changelog.

## Roadmap updates

No roadmap items were checked off — today's work was documentation site updates, not language implementation. The roadmap's "Last updated" date was advanced to 2026-07-25. A working note was added recording the documentation site sync sprint.

## What's next

**Yesterday's predictions vs. reality:**
1. "Expression receivers" — **not addressed**. Remains the first 0.3.3 scope item.
2. "Array-return lowering" — **not addressed**. Remains a 0.3.3 target.
3. "Generic-function lowering" — **not addressed**. Remains a 0.3.3 target.
4. "README content audit" — **partially addressed** via the doc site, not the README itself. The site now reflects v0.3.2 capability, but `README.md` feature sections still describe 0.2.0-era scope as flagged yesterday.

Today's work was documentation rather than implementation. The 0.3.3 implementation targets remain untouched.

**Most likely next moves:**
1. **0.3.3 implementation** — expression receivers, array-return lowering, and generic-function lowering are the three scoped items. Expression receivers would unlock chained operator expressions. Array-return lowering fixes the same ABI shape as the struct-return bug. Generic-function lowering would bring `T: GeneName` bounds to the JIT.
2. **Site branch merge or deployment** — the `site` branch now has 7 new commits ahead of its last sync point. Whether this deploys via a CI pipeline or manual merge is unclear from the evidence.
3. **README feature audit** — still pending. The README feature sections predate gene/phen, pattern matching, and operator overloading.

---

**Matrix:** 380 PASS / 0 FAIL (unchanged from yesterday).
**Evidence sources:** `git log --all --since="24 hours ago"` (9 commits total: 7 author work + 2 daily log automation), `git diff` and `git show` on each commit, `git diff origin/main..origin/site --stat`, file contents of `gene-phen.mdx` and commit messages, yesterday's daily log from `origin/daily-log-2026-07-24`, `docment/ROADMAP.md`, matrix run output.
