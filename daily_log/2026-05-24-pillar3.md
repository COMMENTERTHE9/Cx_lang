# Daily Status — 2026-05-24 (Deep Dive, Pillar 3: Architecture)

- **State:** `submain@22015c4` in sync, no drift, no submain commits; build/test state unchanged from established baseline (243/0·418/0·189·120/69/0) so skipped redundant re-run for a read-only pillar. Deliverables on `deep-dive-pillar-3`.
- **Central finding (T1.1/R1):** interpreter (tree-walks SemanticProgram) and JIT (SemanticProgram→IR→Cranelift) share NO semantic core below SemanticProgram — every operation's semantics implemented twice. This is the structural cause of all 13 Pillar-2 disagreements; drift surface grows with every feature. #1 risk.
- **Builtins in 4 modules (T1.2/R2):** runtime.rs, semantic.rs, ir/validate.rs, ir/lower.rs hardcode the builtin set as string literals, no single source of truth → silent backend mismatches. Needs a registry.
- **Perf=architecture (T1.4/R3):** the interpreter discards the `BindingId` the semantic phase computes — `VarRef` uses only `name`, `get_var` does SipHash HashMap<String> lookup (runtime.rs:677,426). Pillar-1's "slot-index locals" is really "use the resolution that already exists." Plus quadruple numeric type representation (T1.3/R4), runtime.rs 1955-line monolith (T2.1/R5), Value/RuntimeError mis-located in frontend/ (T2.2).
- **Delivered:** `report/architecture.md` (tiered, file:line cited) + `report/architecture_risks.md` (R1–R5 with 0.2 sequencing). Cited Pillar 1/2 findings as architecture causes per framing. No code changed. **For Zara → backend dev:** host_boundary.rs (8944L) and ir/lower.rs (8019L) are large; R1 (shared semantics) and the Cat-A JIT safety gaps are backend-owned. Next: Pillar 4 (DX) + final synthesis.
