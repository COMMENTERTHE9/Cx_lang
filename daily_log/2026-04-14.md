# Cx Daily Log — 2026-04-14

## Snapshot

Two-phase day. The first half of the UTC window was a big submain push — six
`COMMENTERTHE9` commits closing Result, per-width overflow, optional semicolons
in function bodies, a diagnostics pass, and an audit of
parser/semantic/interpreter agreement (all landed 2026-04-13 in EDT, which
straddles this 24h window in UTC). The second half — everything after ~01:01
UTC — was idle: no new work commits, clean working tree, no uncommitted diffs.
Matrix is 78/78 on `main` and 110/110 on `submain`. `main` has not moved since
March 31; the submain→main integration backlog now stands at 14 work commits.

## Landed today

All real work in this window is on `origin/submain`. `origin/main` is unchanged.

- **CONFIRMED — `eb65acf` (2026-04-13 23:45 UTC): parser body-gap fix + audit
  Part 1.** `src/frontend/parser.rs` `body_stmt_tagged` was missing
  `for_stmt`/`while_stmt`/`loop_stmt`/`break_stmt`/`continue_stmt`, so these
  parsed at top level but were rejected inside function bodies. Fix: a 5-line
  addition to the body-statement `choice`. New matrix tests `t104`–`t108` cover
  for/while/loop/break/continue inside function bodies. Same commit adds 12
  discovery programs under `examples/audit/` with a triage report
  (`AUDIT_REPORT.md`): 7 PASS, 1 PARSE_FAIL (`Result<Result<T>>` —
  non-recursive `type_parser`), 1 WRONG_OUTPUT (audit_09 — struct-field `+=`
  skips width truncation), 1 PANIC (audit_11 — `fib(8)` exhausts the Rust
  stack from the tree-walking interpreter's per-call-frame footprint), 2
  SEMANTIC_FAIL from by-design untyped-assignment rejections. Triage tiers
  explicitly annotated must-fix / should-fix / known-limit / defer-post-0.1.
  Matrix went 105/105 → 110/110 on submain.

- **CONFIRMED — earlier 2026-04-13 sprint (boundary of this 24h window).**
  These land just before the strict 24h boundary but belong to the same
  development session and are captured here as integration context.
  - `45f35c5` — diagnostics readability pass: `type_name` helper maps
    `SemanticType` → surface syntax, 12 of 13 `sem_err!` sites rewritten,
    several runtime error strings repaired. t101–t103 pin the rewritten
    messages.
  - `6c27da0` — function-body semicolons optional, implicit return preserved.
    Parser tags each body statement with semicolon presence so only a trailing
    `ExprStmt` without `;` is promoted to an implicit return. t97–t100 (with
    t99 a deliberate expected-fail for `expr;` suppressing implicit return).
  - `dc564a7` — integer overflow at declared width. `apply_numeric_cast` runs
    inside Binary/Unary evaluation, `common_numeric_type` picks the wider
    declared integer type, integer literals adopt surrounding declared width.
    t89–t96 cover t8 add/mul/chain/compare/neg, t16 wrap, t128 pass-through,
    mixed-width promotion.
  - `25e26f0` — examples polish + warning cleanup + roadmap v4.7 bump.
    `examples/run_all.sh` reports 8/8 PASS.
  - `02178a2` — minimal error model: `Result<T>`, `Ok`, `Err`, postfix `?`.
    New lexer keywords, AST nodes (`Type::Result`,
    `Expr::ResultOk/ResultErr/Try`), semantic enforcement (`?` only inside
    `Result`-returning functions, `Err` argument must be a string), runtime
    unwrap + `EarlyReturn` propagation, `Value::ResultOk/ResultErr` with
    stringification and `PartialEq`. t81–t88.
  - `3aa28f1` / `2aa72fa` — Phase 10 `for` loop lowering (4 tests) + a
    regression verifying conditional return inside a while loop lowers
    correctly.

- **CONFIRMED — `b3968ec` (2026-04-14 00:06 UTC) + `29e2c3f` (2026-04-14 01:01
  UTC): automated daily-log and blog commits** for 2026-04-14. The daily-log
  commit touched `daily_log/2026-04-14.md` and `docs/frontend/ROADMAP.md`;
  the site commit added `src/content/blog/2026-04-14.mdx`. Documentation, not
  engineering.

- **No merges, no movement on `main`.** `origin/main` is still at `87d68f6`
  (2026-03-31). PR #27 remains the last successful submain→main integration.
  The daily-log branch backlog is 13 branches deep through
  `daily-log-2026-04-12`, plus this branch.

## In progress / uncommitted work

Working tree is clean: no staged diffs, no unstaged diffs, no untracked files
(verified at 23:34 UTC). `.claude/worktrees/` does not exist in this repo.
`find src/ docs/ -newer …` surfaces nothing on `main`.

The meaningful in-progress surface is carried over from `AUDIT_REPORT.md` and
is unchanged since it landed:

- **audit_09 residual** — struct-field compound assign skips width truncation.
  The Binary/Unary fix from `dc564a7` does not reach the `CompoundAssign` arm
  in `run_semantic_stmt`. Known mechanical fix, not yet committed. Keeps the
  "integer overflow enforced" hard blocker open.
- **audit_02 residual** — `result_type` combinator is non-recursive, so
  `Result<Result<T>>`, `Handle<Result<T>>`, `[N: Handle<T>]` and any other
  nested type composition fail to parse. Shape argues for a `recursive()`
  refactor of `type_parser`.
- **Result in IR lowering** — the 2026-04-12 Phase 10 commit added
  `unsupported!` arms for `ResultOk`/`ResultErr`/`Try` and
  `SemanticType::Result(_)`. Today's `02178a2` implements Result in the
  interpreter path only; the lowering-side placeholders still throw.
  Tagged-union lowering over the existing enum layout is the likely direction.

## Decisions and direction

No new design decisions since the late-sprint commits yesterday. Those commits
committed to implementation shapes already reflected in the locked spec —
overflow truncating at the expression boundary (spec catching up to itself),
trailing-expression-is-meaningful grammar (`6c27da0`), and a usable audit
triage tier system for 0.1 (`AUDIT_REPORT.md`). No spec or syntax-decision
docs changed after `25e26f0`'s v4.7 bump.

## Roadmap updates

`docs/frontend/ROADMAP.md` was already updated earlier in the UTC day (in
`b3968ec`) and those edits still match the evidence:

- Hard Blockers — test runner, minimal error model, and `examples/` gate all
  checked off.
- Hard Blockers — integer overflow and semicolons both left UNCHECKED, which
  is correct: the struct-field compound-assign gap (audit_09) and the
  expression-statement semicolon requirement are both still open.
- Active — Phase 10 entry expanded for `loop`/`break`/`continue` (2026-04-12)
  and `for` (2026-04-13).
- Active — new entry for the parser/semantic/interpreter agreement audit
  pointing at `examples/audit/`.
- Known Gaps — overflow and semicolons rewritten to reflect the partial
  closures.
- New "Key Changes from v4.8 (working, submain)" section preserved.

No further roadmap edits are justified in this refresh — nothing new has
landed since that commit. Leaving the roadmap content unchanged from the
earlier UTC pass.

## What's next

1. **Fix audit_09 and actually close the overflow hard blocker.** Call
   `apply_numeric_cast` in the `CompoundAssign` arm of `run_semantic_stmt`
   using the field's declared `SemanticType`. Promote the audit_09 shape
   into a matrix regression. Cheapest remaining move.
2. **Convert `result_type` to `recursive()`** (audit_02). Same refactor
   unlocks `Handle<Result<T>>` and any nested composition.
3. **Implement `Result<T>` in IR lowering.** The interpreter path is done;
   the `unsupported!` arms in `lower.rs` for `ResultOk`/`ResultErr`/`Try`
   and `SemanticType::Result(_)` still throw. Tagged-union lowering is the
   natural shape.
4. **Merge submain → main.** Fourteen work commits unmerged plus a growing
   daily-log branch backlog. `docs/frontend/ROADMAP.md` (main at v4.8,
   submain at v4.7) will conflict on merge; every additional day makes the
   resolution surface larger.
5. **Audit Part 2.** Arrays, imports + generics, nested generics, and
   `copy_into` under `Result` are plausible next discovery surfaces.

**Yesterday (2026-04-14 early-UTC log) vs now.** The earlier 2026-04-14 log
(committed at 00:06 UTC, covering the sprint that had just landed) predicted
(1) fix audit_09, (2) fix audit_02, (3) merge submain→main, (4) implement
Result in IR lowering, (5) audit Part 2. None happened in the second half of
the UTC day. Direction is unchanged; the integration debt is unchanged; the
three triaged audit must-fixes are unchanged.

**Matrix.** 78 PASS / 0 FAIL / 78 total on `main` (verified via
`bash src/tests/run_matrix.sh`). Submain, per the audit commit message, is
at 110/110 — not re-verified here because this checkout is on `main`.

**Evidence sources used:** `git fetch --all --prune`,
`git log --all --since="24 hours ago"` (with and without `--merges`, with
and without `--author`), `git log origin/main..origin/submain`, `git show`
for each commit in the window, `git status`, `git diff`, `git ls-files
--others --exclude-standard`, `find` across `src/` and `docs/`, `bash
src/tests/run_matrix.sh` on `main`, prior daily log at
`origin/daily-log-2026-04-12` and the earlier 2026-04-14 log captured in
`b3968ec`, and `docs/frontend/ROADMAP.md` on the daily-log branch.
