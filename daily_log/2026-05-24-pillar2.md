# Daily Status — 2026-05-24 (Deep Dive, Pillar 2: Correctness)

- **State:** `submain@22015c4` in sync with origin, no drift; investigation on `submain` (read-only), deliverables on `deep-dive-pillar-2`. Built both debug binaries for differential probing (default=interp, `--backend=cranelift`=JIT).
- **Method:** differential probing (interp vs JIT, stdout+exit). 30+ hand-written probes; 14 minimal repros kept in `report/pillar2_repros/` (NOT in verification_matrix — would break the green 189/0 baseline; flagged for promotion when fixed).
- **Top finding (shipped interpreter):** semantic analysis does not type-check composite-literal contents — struct literals accept missing fields (B1), wrong-typed fields (B2: `b: t64 = "hello"` prints "hello"), unknown fields (B3); array element types unchecked (B4: `[1,"two",3]` for `[3:t64]`). Type-soundness holes that run silently. One root fix in `semantic.rs`.
- **JIT crash/soundness wall (Category A, 6 findings):** `f64→t8/t16` cast panics codegen (A1); array OOB read→garbage (A2), write→SIGSEGV (A3), neg index→garbage (A4); `INT_MIN/-1`→SIGFPE (A5); runtime div-by-zero→SIGILL (A6). Interpreter traps all cleanly — JIT omits the checks. Plus A7: deep recursion SIGABRTs the interpreter (uncatchable) while JIT succeeds.
- **Also:** misleading error messages (array-OOB routed through "variable not declared" — D1, worst offender; i128-overflow literal blamed on "invalid character" — D3) with proposed rewrites; design footguns (`if <unknown>`→else silently, non-exhaustive `when` no-ops) listed for a 0.2 ruling. Full writeup `report/correctness.md`. No code changed (proposed fixes ripple beyond small-fix scope). **For Zara → backend dev:** Category A (A1–A6) is backend-owned and gates JIT-as-default. Next: Pillar 3 (Architecture).
