# Cx Daily Log — 2026-05-02

## Snapshot

The main story today is housekeeping, not features. A batch of ~24 stale daily-log PRs (dating back to 2026-03-29) were merged into main, clearing a backlog that had been flagged repeatedly in prior daily logs. The only real code-facing commit was a documentation-only change on a new feature branch (`stokowski/cx-6-document-unary-lowering`) that adds block-level comments to the unary lowering logic in `src/ir/lower.rs`. No new features, no test changes, no roadmap items completed. Matrix holds at 78/78 on main.

## Landed today

- **CONFIRMED** — Daily-log PR backlog cleared. PRs #29 through #52 were merged to main in a single batch (17:45–18:14 EDT). These cover daily logs from 2026-03-29 through 2026-05-01, plus their associated `docs/frontend/ROADMAP.md` updates. This was pure housekeeping — no language code changed through these merges, only `daily_log/*.md` files and incremental roadmap edits.

- **CONFIRMED** — `2d665a4` on `origin/stokowski/cx-6-document-unary-lowering` (2026-05-02 19:29 EDT): CX-6 documentation commit. Added 25 lines of block-level and inline comments to `src/ir/lower.rs` explaining the unary lowering strategy. Documents that `Op::Minus` lowers to `0 - value` (no dedicated negate IR instruction) and `Op::Not` lowers to `value == 0` (Bool equality against zero). No behavioral change — comments only. This commit is **branch-local** and has not been merged to main or submitted as a PR.

The `stokowski/cx-6-document-unary-lowering` branch carries 22 commits not on main (inherited from the submain lineage — Phases 10–11 IR backend work, audits, error model, etc.). Only the top commit (`2d665a4`) is new today; the remaining 21 are older submain work. No integration risk from the documentation commit itself, though the branch as a whole diverges significantly from main.

## In progress / uncommitted work

Nothing to report. Working tree is clean (detached HEAD at `345751b`, which is `origin/main`). No staged, unstaged, or untracked changes. No recently modified files in `src/`, `docment/`, or `.claude/worktrees/` beyond what's already committed.

## Decisions and direction

**Daily-log PR backlog resolved.** Yesterday's log (and several before it) noted "Merge or close stale daily-log PRs" as growing housekeeping debt. Today that debt was cleared in bulk. This doesn't change language direction, but it means the main branch now has a continuous record of daily logs from late March through today.

**CX-6 issue: documentation-first approach for IR lowering.** The `stokowski/cx-6-document-unary-lowering` branch signals that the project is investing in documenting the IR lowering strategies before moving to the next implementation sub-packet. The comments explain *why* the IR has no unary negate or not instructions (both are lowered to two-operand forms so backends only handle one instruction shape). This is a reasonable call — the encoding choices are non-obvious, and documenting them now prevents confusion when struct field lowering or more complex expression lowering arrives next.

**Submain divergence remains the dominant structural issue.** 22 commits, now ~35 days. The `stokowski` branch was forked from submain (or equivalent), meaning new feature branches are building on the submain lineage rather than main. Main is still at v4.9 / 78 tests; submain carries v5.0 / 117 tests with all hard blockers resolved. Until the merge happens, the main branch roadmap and the actual state of the language continue to drift apart.

## Roadmap updates

No items checked off — today's work was documentation and housekeeping, not feature completion.

No new tasks added — the natural next steps (struct field access lowering, struct literal lowering, submain merge) are already captured or implied by existing Active entries.

One minor edit: added a working note recording the daily-log PR backlog clearance and the CX-6 documentation branch.

## What's next

**Yesterday's predictions vs. what happened:**

1. "Merge submain to main" — did NOT happen. Now ~35 days diverged.
2. "Struct field access lowering" — did NOT happen. The CX-6 documentation commit suggests preparation rather than implementation.
3. "Struct literal lowering" — did NOT happen.
4. "Merge or close stale daily-log PRs" — YES. This was the one prediction that was confirmed, and it was done comprehensively.

**Next steps, ranked by leverage:**

1. **Merge submain to main.** This remains the single highest-leverage action and has been for over a month. The `stokowski` branch forking from submain (not main) underscores that active development has moved past what main reflects.
2. **Land CX-6 documentation PR.** The documentation commit on `stokowski/cx-6-document-unary-lowering` is ready for a PR. Small, safe, comments-only.
3. **Struct field access lowering.** The struct registry and Ptr infrastructure on submain are ready. `DotAccess` on struct fields using field offsets from `StructLayoutInfo` is the natural next Phase 11 sub-packet.
4. **Struct literal lowering.** `SemanticExprKind::StructLit` lowering to Alloca + per-field Store instructions using layout offsets.

---

**Matrix status:** 78 PASS / 0 FAIL / 78 total on main. Unchanged from yesterday.

**Evidence sources used:** `git fetch --all --prune`, `git log --all --since="24 hours ago"` (all commits, author-filtered, merge/non-merge, name-only), `git show 2d665a4` (full diff of CX-6 documentation commit), `git log origin/stokowski/cx-6-document-unary-lowering --not origin/main` (22 branch-local commits), `git merge-base` (branch divergence point), `git status`, `git diff` (all variants), `git ls-files --others`, `find` for recently modified files, `run_matrix.sh` output (78/78), yesterday's daily log (`daily_log/2026-05-01.md`), roadmap at `docs/frontend/ROADMAP.md` (v4.9).
