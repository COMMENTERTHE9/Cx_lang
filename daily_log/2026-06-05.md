# Cx Daily Log — 2026-06-05

## Snapshot

A release-prep day. The README on submain now declares v0.2.0 released (dated today), though the git tag has not been pushed yet. Two quality-oriented commits landed on submain: a first-user audit report documenting a structured walkthrough of the "clone, build, write a program" experience, and a backend build-warning fix that closes tracker #042. On main, 12 daily-log PRs (#265, #274–286) were merged in a single batch, clearing the May 16–27 documentation backlog. Submain remains 35 non-merge commits ahead of main with 157 changed files. Matrix on main holds steady at 182/0.

## Landed today

- **Backend default-build warnings fixed** (`ae410e1`, tracker #042, `origin/submain`) — CONFIRMED. Closes first-user audit finding F7: a default `cargo build` (without `--features jit`) emitted two warnings. The `IrType` import in `cranelift/mod.rs` was gated behind `#[cfg(feature = "jit")]` since it's only used by `ir_type_to_cranelift`. `ArrayLayout::total_size` and `alignment` in `ir/types.rs` received `#[allow(dead_code)]` annotations matching the existing `element_size` annotation — both fields are read by the Cranelift backend and unit tests but not by the default interpreter build path. The stale comment in `validate.rs` `is_reserved_runtime_intrinsic` was corrected: it claimed `exit` was the lone `validator_reserved=false` holdout, but tracker #033 already closed that exception. No behavior change. 3 files, +15/−5.

- **First-user audit report** (`229e6fc`, tracker #029, `origin/submain`) — CONFIRMED. A 177-line structured protocol answering "can a person who has never seen Cx clone the repo, build it, and write a working program following only the README in under 30 minutes?" Documents the audit phases (prerequisites, clone, build, run examples, write original code) and catalogues friction findings. Key findings include: README claimed zero warnings but the default build path emitted two (F7, now fixed by #042), README described v0.1.0 stats while HEAD was mid-0.2 (F8), and the canonical `hello.cx` example uses string interpolation that the README's own deferred list says isn't in the release (F9). The report is in `report/first_user_audit.md`.

- **v0.2.0 release stamp in README** (`0e84ca5`, `origin/submain`) — CONFIRMED. Changes the status line from "0.2, in development (unreleased)" to "0.2.0 — released 2026-06-05" and updates the last tagged release reference from v0.1.0 to v0.2.0. Updates the verification status header from "As of `submain` at `c3506c6`" to "As of `v0.2.0`". The v0.2.0 git tag does not exist yet — the stamp is declared but the tag has not been pushed.

- **12 daily-log PRs merged to main** (PRs #265, #274–286) — CONFIRMED. Integration context: all 12 PRs were merged within a ~20 minute window (18:41–19:02 EDT). These brought daily logs from May 16 through May 27 and associated roadmap updates into main, clearing a backlog that had grown since the last daily-log merge on May 9. The roadmap on main was updated as part of these merges but some corrections (Phase 11 check-offs) were not carried through.

All three real-work commits are on `origin/submain` only — none have been merged to main.

## In progress / uncommitted work

Nothing to report. The working tree on main is clean. All meaningful work is committed.

The primary in-progress state is the submain-to-main divergence: 35 non-merge commits spanning tracker items #001 through #042, plus the entire v0.2 feature sprint (string concatenation, `len` builtin, if-as-expression, enum signatures, TBool safety, unknown-as-value error, runtime refactoring, diagnostic improvements, performance work). 157 files changed, +4138/−2928. The README on submain declares this as v0.2.0 released, but submain has not been merged to main and the tag has not been pushed. The release is declared but not yet fully enacted.

## Decisions and direction

**v0.2.0 declared.** The README stamp is the strongest signal of release intent so far. The verification numbers accompanying it — 243 unit tests, 418 with JIT, 230 fixtures, 140 PASS / 90 SKIP / 0 PARITY_FAIL, zero Clippy errors — represent a substantial quality gate. The tag is missing, and the merge to main hasn't happened, so the release is in a partially completed state.

**First-user audit as quality gate.** The audit report represents a structured usability review — not just test counts but "can someone actually use this?" The friction findings it surfaced (stale README stats, default-build warnings, documentation/reality mismatches) were immediately acted on: tracker #042 fixed the warnings today. The audit establishes a baseline expectation that Cx should be approachable from the README alone, and its findings will likely drive further documentation cleanup.

**Build warning tolerance set to zero.** The #042 fix is small but signals a policy: the default build path must be warning-free. The approach — cfg-gating JIT-only imports, `#[allow(dead_code)]` for cross-feature fields — is pragmatic and matches the existing codebase style.

## Roadmap updates

**Checked off:**
- Phase 11 — `when` block lowering (`bed71c1`): confirmed landed on main, was unmarked in the roadmap
- Phase 11 — DotAccess in compound forms (`2d6804a`): confirmed landed on main, was unmarked in the roadmap

**Updated:**
- Phase 15 JIT parity numbers updated from "110 PASS / 72 SKIP / 0 PARITY_FAIL across 182 fixtures" to "140 PASS / 90 SKIP / 0 PARITY_FAIL across 230 fixtures on submain" — reflecting the current leading-edge state
- Working notes section: added 2026-06-05 entry documenting today's activity
- Last-updated date set to 2026-06-05

**Not changed:**
- Phase 11 remains `[ ]` overall (all sub-items now checked, but the parent item may need additional work to close formally)
- Phase 8 Round 2 (str/strref layout, Handle<T>) remains incomplete
- Phases 12, 9, 14, 15 status unchanged beyond the parity number update
- No new roadmap tasks added — today's work was quality and release prep, not new feature development

## What's next

**Yesterday's predictions vs. today:**
1. *"Merge submain -> main"* — DID NOT HAPPEN. Fourth consecutive day this has been the top prediction. Submain grew from 31 to 35 non-merge commits ahead. However, the v0.2.0 release stamp strongly suggests this merge is imminent.
2. *"Continue the tracker sprint"* — CONFIRMED. Three more commits landed: the audit report (#029), the build-warning fix (#042), and the v0.2.0 README stamp.
3. *"Backend phases"* — NOT ADDRESSED. No backend feature work happened today.

**Likely next moves:**
1. **Tag v0.2.0 and merge submain -> main.** The README already declares v0.2.0 released. The natural next step is pushing the v0.2.0 tag and merging submain to main. This is the most overdue action — submain is 35 commits and 157 files ahead, and the release is declared but incomplete without the tag and merge.
2. **Merge remaining daily-log branches.** Eight daily-log branches (May 28 through Jun 4) remain unmerged to main. The May 16–27 batch was cleared today.
3. **Address remaining first-user audit findings.** F8 (stale README version numbers) is partially addressed by the v0.2.0 stamp but the deferred-features list may still be stale. F9 (hello.cx uses undocumented string interpolation) may need a documentation update or example change.

---

**Matrix (main):** 182 PASS / 0 FAIL out of 182 total — unchanged from yesterday.

**Reverts:** None in the last 24 hours.

**Evidence sources:** `git log --all --since="24 hours ago"` (3 real-work commits on submain, 22 daily-log merge commits on main, 1 daily-log commit on daily-log-2026-06-04), `git log origin/submain --not origin/main` (35 non-merge commits ahead), `git diff --stat origin/main..origin/submain` (157 files, +4138/−2928), `git show` for commits `ae410e1`, `229e6fc`, `0e84ca5` (full diffs inspected), `git tag -l` (only v0.1.0 exists), `git status` (clean working tree), `run_matrix.sh` (182/0 on main), yesterday's daily log from `origin/daily-log-2026-06-04`, `docment/ROADMAP.md`, `report/first_user_audit.md` (top 60 lines inspected).
