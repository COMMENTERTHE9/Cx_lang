# Cx Daily Log — 2026-03-22

## Snapshot

The most productive day on the Cx project so far. 31 non-merge work commits landed across submain, covering language features (f64, const, value-producing when), a major code quality sprint, dead code removal of the old AST interpreter, doc consolidation, roadmap rewrite, and the beginning of backend IR function call lowering. Six PRs (#8 through #13) merged into main, bringing main current with nearly all of submain's history. The verification matrix grew from 54 to 59 tests, all green. Four hard blockers for 0.1 were closed in a single day.

## Landed today

**Language features — CONFIRMED**

- **f64 type keyword — full pipeline** (7c1768d): `f64` surface keyword added to lexer, parser, semantic layer, and runtime. New test t55_f64_basic.cx. This was a hard blocker for 0.1.
- **const declarations and pub keyword** (a7ece34): `const` declarations with literal-only initializers. Semantic pass rejects reassignment. `pub` keyword for export visibility. Tests t56 (const basic) and t57 (const reassign reject, expected_fail). Another hard blocker closed.
- **Value-producing when — full pipeline** (d24b462): `when` blocks can now be used as expressions (`let x = when y { 1 => 10, _ => 0 }`). Touches AST, parser, semantic layer, IR lowering, and runtime. Test t59_when_value.cx. Hard blocker closed.
- **when block-body arms verified** (e0f989e): Block bodies in when arms confirmed working. Test t58_when_block_body.cx. Known gap resolved.
- **SemanticType::Void** (642a327): Void function calls now type correctly through the semantic layer. Fixes void function call typing that was blocking function call lowering in the IR.

**Code quality sprint — CONFIRMED**

- **Arc\<SemanticFunction\>** (0c0cb8a): Function bodies stored as Arc, eliminating clone-on-every-call overhead.
- **sem_err! macro** (f8e021d): 51 SemanticError constructions collapsed to 1-line macro calls in semantic.rs.
- **unsupported! + unsupported_type! macros** (5819185, 14c63e5): 35 arms collapsed in ir/lower.rs.
- **value_to_string unification** (878029a): print_value and print_value_inline merged into a single value_to_string function.
- **semantic_type_to_ast duplicate removed** (0c0cb8a): Replaced with From\<SemanticType\> for Type.
- **Old AST interpreter deleted** (cdffec7): eval_expr (~314 lines) + run_stmt (~462 lines) removed. ~790 lines of dead code gone.
- **Dead code deletion** (cf7b775): FuncDef, funcs, impls, register_func — confirmed dead code removed from diagnostics, main, and runtime.

**Housekeeping — CONFIRMED**

- **Test matrix renumbered** (d8cf3ef): 10 duplicate test number pairs fixed, 2 gaps closed. Matrix now runs 54 tests cleanly (before new tests were added).
- **Docs consolidated** (f3b1349): `docment/` and `document/` merged into `docs/`. Single source of truth.
- **Stale files cleaned** (183e0f6, 5e82d34, 50fdc2c): _tmp test file, stale daily-log branch, gitignore contradictions, .jj/ added to gitignore.
- **CI fix** (76cf9d1, 100356b): `set +e` added to matrix runner to prevent early exit on expected failures.

**Backend IR progress — CONFIRMED**

- **Phase 6 Stage 2b: function call lowering** (a6d7701): Direct function call lowering with arity and type validation in ir/lower.rs. 270+ lines of new lowering code.
- **Phase 6 Stage 3: cross-function call validation** (ee2c357): IR validator now resolves callees, checks arity, and validates argument types against function signatures. 62+ lines in ir/validate.rs.
- **Stale backend_plan.md removed** (1fa9aae): Superseded by cx_backend_roadmap_v3_1.md.

**Documentation — CONFIRMED**

- **Backend roadmap v3.2** (4738ed9): Cross-roadmap dependencies, TBool design notes, Phase 0.5, copy param convention, when blocks, range/loop dependency added.
- **Frontend roadmap v4.3** (e1ac378, 8eac111): Syntax decisions locked, hard blockers rewritten, known gaps expanded.

**Integration — CONFIRMED**

- PRs #8 through #13 all merged into main. Main is now current with submain except for the 3 most recent commits (Phase 6 Stage 2b, Stage 3, and backend_plan.md removal).

## In progress / uncommitted work

Working tree is clean. No uncommitted changes.

Three commits on submain are not yet on main:
- ee2c357 — Phase 6 Stage 3: cross-function call validation
- a6d7701 — Phase 6 Stage 2b: function call lowering
- 1fa9aae — docs: remove stale backend_plan.md

These represent active backend IR work that will likely merge in the next PR.

## Decisions and direction

**Four hard blockers closed in one day.** The f64 surface keyword, const declarations, value-producing when, and when block-body arms all shipped. The hard blocker list in the roadmap dropped significantly — the remaining blockers are multi-file imports, print-as-function, UTF-8 decision, and the structural/testing gates.

**Docs consolidated to `docs/`.** The long-standing split between `docment/` and `document/` is resolved. All documentation now lives in `docs/` with subdirectories for frontend and backend roadmaps.

**Backend IR is catching up to the frontend.** With function call lowering (Stage 2b) and cross-function validation (Stage 3) landing, the IR backend is starting to handle the constructs that the frontend has supported for weeks. This is the first day with substantial backend IR progress since Phase 6 began.

**Roadmap v4.3 locks syntax decisions.** The frozen syntax section is now the canonical reference for all locked decisions. No breaking changes after 0.1.

## Roadmap updates

**Checked off (moved to Done or resolved):**
- f64 type keyword — was a hard blocker, now complete with surface syntax
- const declarations — was a hard blocker, now complete
- Value-producing when — was a hard blocker, now complete
- when block-body arms — was a known gap, now verified with t58
- Code Quality Sprint section already existed in Done, confirmed complete

**Updated in Active:**
- Backend IR Phase 6 updated to reflect Stage 2b/3 progress (function call lowering and validation)
- f64 surface keyword removed from Active (complete)

**Known Gaps updated:**
- when block-body arms removed (resolved)

**No new roadmap items added.** Today's work was execution on existing items, not discovery of new ones.

## What's next

1. **Merge submain to main.** Three commits (Phase 6 Stages 2b/3 and doc cleanup) are ready. This should be the next PR.

2. **Backend IR Phase 6 continuation.** With function calls lowering and validating, the next likely targets are loop lowering (while, for-range) and struct/method call lowering in the IR.

3. **Multi-file imports.** This is the largest remaining hard blocker. The `#![imports]` syntax is locked, but the module resolution, pub enforcement, and circular import detection are not yet implemented.

4. **print promoted to function.** Another hard blocker — print and printn need to become real function calls rather than statement-level constructs. Should happen before the backend locks calling convention.

5. **Matrix at 59/59.** All green. No regressions.
