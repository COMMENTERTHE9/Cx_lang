# Cx Daily Log — 2026-08-12

## Snapshot

Day ten of the development pause. No developer-authored commits on any branch in the last 24 hours. The two commits in the 24-hour window are both automated Claude sessions: yesterday's daily log and a retroactive blog post on the site branch. Working tree is clean. Matrix holds at 380/0 on main. Submain remains 27 commits ahead of main, unchanged since Aug 2. The open daily-log PR backlog has grown to 79 open PRs, spanning May 28 through Aug 11, none merged.

## Landed today

No development work landed on any branch in the last 24 hours.

The two commits in the window:

- **CONFIRMED** — `3718b46` on `origin/daily-log-2026-08-11`: "daily log 2026-08-11" (automated log commit, PR #371 opened). Updates `daily_log/2026-08-11.md` and a date bump in `docment/ROADMAP.md`. Not development work.

- **CONFIRMED** — `fb0dca5` on `origin/site`: "dev log 2026-08-03" (automated site blog post). Adds `src/content/blog/2026-08-03.mdx`, a retroactive write-up of the cycle-only call-depth guard optimization that originally landed on submain during the Aug 2–3 session. The blog post documents how `functions_that_can_recurse()` in `host_boundary.rs` uses Tarjan's SCC algorithm to restrict depth guards to recursive call paths only, with measured results showing non-recursive code paying zero overhead. This is documentation of past work, not new development.

## In progress / uncommitted work

Nothing to report. Working tree is clean on a detached HEAD at main (`54ab720`, tagged `v0.3.2`). No staged, unstaged, or untracked changes.

The outstanding submain backlog remains the dominant pending item — **27 commits ahead** of main, unchanged for ten days. The scope includes:
- Generic-function monomorphization and generic-struct instantiation lowering
- Gene-bound soundness fix (reject field access through a type-parameter bound)
- Interpreter and JIT call-depth guards, cycle-only guard optimization
- Const lowering (closes known-issues #12), exit lowering (closes known-issues #11)
- Enforcement-layer fixes (const immutability, field/variant/loop-counter facts)
- Rejection-shape harness
- Multidimensional arrays roadmap + design gate
- Paired documentation commits for each feature
- README accuracy passes

None of these have merged to main. None tagged.

## Decisions and direction

No new design decisions. No spec or design-doc changes in the last 24 hours. No files modified in `src/`, `docment/`, or `.claude/worktrees/` within the past day.

The site branch received a retroactive blog post covering Aug 3's cycle-only guard optimization. This documents an architectural decision already made: restricting JIT call-depth instrumentation to functions participating in call cycles (via SCC analysis), trading ~130 lines of call-graph code for zero overhead on non-recursive paths. The decision and its measurements were already recorded in submain's documentation commits; the blog post is a public-facing summary, not a new direction.

The PR backlog continues to grow. **79 open PRs** now span from #287 (May 28) through #371 (Aug 11). All but one (#347, a README version update) are daily log PRs. None have been merged. This is strictly documentation and does not block development, but the accumulation is now substantial — nearly three months of daily logs sitting in unmerged PRs.

## Roadmap updates

Updated `Last updated` date to 2026-08-12. No items checked off — no new work landed. No new tasks added — no evidence justifies additions.

The submain copy of the roadmap remains more current than main's copy. Those updates (post-0.3.2 section, remaining lowering blockers, multidimensional arrays design gate, version 0.5 framing) belong with the submain merge, not applied independently to main's roadmap.

## What's next

**Yesterday's predictions vs. today:**
1. "Merge submain to main" — not done. Predicted every day since late July.
2. "Array-return lowering" — not done.
3. "Expression receivers" — not done.
4. "Batch-merge daily log PRs" — not done.

None of the predicted next steps happened, continuing the pattern for ten consecutive days.

**Most likely next moves (when activity resumes):**
1. **Merge submain to main.** 27 additive commits, no conflicts visible. This remains the clear first move to bring code, tests, docs, and roadmap updates into main together.
2. **Array-return lowering.** First remaining 0.3.3 implementation item after submain lands.
3. **Expression receivers.** Second remaining 0.3.3 implementation item.
4. **Batch-merge daily log PRs.** 79 open PRs (#287–#371), documentation-only, growing daily.

---

**Matrix (main):** 380 PASS / 0 FAIL — unchanged from Aug 11 and the past ten days.
**Matrix (submain, per roadmap):** 414/414 — as recorded in `e3edab5`.
**Submain divergence:** 27 commits ahead of main (unchanged from Aug 11).
**Reverts:** None.
**Open PRs:** 79 total (#287 through #371). 78 daily log PRs (May 28–Aug 11) + 1 README update (#347). All unmerged.
**Evidence sources:** `git log --all --since="24 hours ago"` (2 automated commits), `git log origin/main..origin/submain` (27 commits), `git status` (clean), `run_matrix.sh` (380/0), `mcp__github__list_pull_requests` (79 open PRs), `git show origin/daily-log-2026-08-11:daily_log/2026-08-11.md` (yesterday's log), `docment/ROADMAP.md`, `git show fb0dca5` (site branch blog post).
