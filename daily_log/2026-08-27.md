# Cx Daily Log — 2026-08-27

## Snapshot

The daily log backlog is cleared. Eighty daily logs spanning May 28 through August 26 were migrated from scattered branch-local commits into main via a single batch PR (#391), and six stale daily-log PRs (#298, #304, #320, #356, #364, #368) that had been stuck behind the stale-base gate were reconciled and merged. The reconciliation also cleaned four superseded roadmap entries from `docment/ROADMAP.md` and one outdated working note. No language or compiler development work landed on any branch today. Submain holds at 9 commits ahead of main (now 11 days old for the earliest commits). A blog post for 2026-08-26 was committed on the `site` branch.

## Landed today

**1. Daily log backlog migration** (`a448107` through `22dae0f`, merged via PR #391 → main) — CONFIRMED

Four commits migrated 80 daily logs from branch-local history into `daily_log/` on main, organized by month:

- May 2026: 4 logs (`2026-05-28` through `2026-05-31`)
- June 2026: 26 logs (`2026-06-01` through `2026-06-30`)
- July 2026: 30 logs (`2026-07-01` through `2026-07-31`)
- August 2026: 20 logs (`2026-08-01` through `2026-08-26`)

These logs had previously existed only on their individual `daily-log-YYYY-MM-DD` branches or on the `site` branch's blog copies. The migration brings them all under version control on main for the first time, making the project's development history accessible from a single branch. Total: 6,699 lines of new content across 88 files (including the CI fix from yesterday).

**2. Six stale daily-log PR reconciliations** (merged via PRs #298, #304, #320, #356, #364, #368 → main) — CONFIRMED

Six daily-log PRs had been blocked by the stale-base CI gate because their branches had fallen behind main. Each was reconciled in a two-step process:

- Merge main into the stale branch to bring it current for the gate
- Reconcile duplicate log content (where two generators had produced separate logs for the same date), dropping superseded roadmap hunks

Affected dates and their reconciliation commits:
- 2026-06-06 (`7f6051f`) — dropped roadmap hunk
- 2026-06-13 (`42fbfcd`) — reconciled duplicate
- 2026-06-27 (`81be5a8`) — dropped roadmap hunk
- 2026-07-26 (`7bc71ca`) — dropped roadmap hunk
- 2026-08-02 (`1cd27f1`) — dropped roadmap hunk
- 2026-08-08 (`c5a5ee1`) — dropped roadmap hunk

The roadmap hunks that were dropped contained three post-0.3.0 checkboxes (CR#1, CR#2, CR#3 — CodeRabbit range-check fixes) and one working note from 2026-06-06. These were superseded by the current roadmap on main and would have created merge conflicts or stale entries if kept. The diff shows 5 lines removed from `docment/ROADMAP.md` total.

**3. Site blog post for 2026-08-26** (`fad7da3`, on `origin/site`) — CONFIRMED (branch-local, not on main)

A 39-line blog post (`src/content/blog/2026-08-26.mdx`) was committed to the `site` branch covering the CI stale-base fix, the daily log reconciliation, and submain divergence status. This commit is branch-local to `site` and has not been merged to main.

## In progress / uncommitted work

Nothing to report. The working tree on main is clean. No uncommitted changes in `src/`, `docment/`, or `.claude/worktrees/`.

**Pending on submain (9 commits, unchanged for 3 days):** Array returns (`65736b5`), expression receivers (`56983eb`), assignment copies (`f291116`), Model B aggregates-as-values (`4fe3fd2`), clippy reconciliation (`d60e508`), Model A contiguous multidimensional arrays (`332a46a`), assignment-target grammar unification (`e943103`, `eb54b36`). The earliest commits (array returns, expression receivers) are now 11 days old, dating back to 2026-08-16 when 0.3.3 shipped.

## Decisions and direction

No language design decisions today. The work was entirely about documentation infrastructure — clearing the log backlog and unblocking stale PRs.

The reconciliation work established a pattern for handling duplicate daily logs produced by concurrent generators: keep the more accurate base, merge corrections from the alternate version, and drop roadmap hunks that conflict with main's current roadmap. This is a process decision, not a language one, but it matters for log integrity going forward.

The roadmap edit (removing 3 CR# checkboxes and 1 working note) was a cleanup of superseded content, not a directional change. The underlying work (CodeRabbit range-check fixes) shipped in 0.2.0 and has been on main since June; the removed entries were stale duplicates that individual daily-log branches had added before the roadmap was reconciled at the 0.3.3 milestone.

## Roadmap updates

The reconciliation commits already removed 5 lines of superseded content from `docment/ROADMAP.md` (3 CR# checkboxes under "Post-release hardening" and 1 working note from 2026-06-06). Those removals landed via PRs #298/#320/#356/#364/#368 and are now on main.

No additional roadmap changes are warranted today. The submain work (9 commits) has not been merged to main, so those items remain correctly listed as 0.4 lowering blockers. No new language or compiler work justifies adding tasks.

## What's next

**Yesterday's predictions vs. today:**
1. "Merge submain to main" — **did not happen.** Now 11 days pending for the oldest commits. This is the longest the gap has been.
2. "Daily log PR backlog" — **confirmed, and resolved comprehensively.** The entire backlog of 80 logs was migrated and 6 stale PRs were cleared. This was the second of yesterday's three predictions and the only one that matched.
3. "Next lowering blocker" — **did not happen.** No language development today.

**Most likely next moves:**
1. **Merge submain to main.** Overdue at 11 days. Nine commits spanning array returns, expression receivers, value semantics, contiguous multidimensional arrays, and assignment-target unification — all tested and clean on submain. The merge would bring main's test count up and close several 0.4 lowering blockers.
2. **Next lowering blocker.** Once the submain merge lands, the remaining 0.4 blockers: `.copy`/`.copy.free`/`copy_into` parameter kinds, nested function definitions, `while-in`, function-body `const`, `t128` printing, `char`, and non-identifier string interpolation.
3. **Site branch maintenance.** The `site` branch diverges further from main with each blog post. At 20+ commits ahead of main, it may need its own integration pass.

---

**Matrix:** 414 PASS / 0 FAIL (verified this session — unchanged from yesterday)
**Submain divergence:** 9 commits ahead of main (unchanged for 3 days; oldest commits now 11 days old)
**Reverts:** None
**Evidence sources:** `git log --all --since="24 hours ago"` (11 non-merge commits, 12 merge commits), `git show` on migration and reconciliation commits, `git diff` on ROADMAP.md changes from reconciliation, `git log origin/main..origin/submain` (9 unmerged commits), `git status` (clean on main), `ls src/tests/verification_matrix/t*.cx` (414 fixtures), yesterday's daily log (`daily_log/2026-08-26.md`), `docment/ROADMAP.md` (read, no additional changes needed), `git show fad7da3` (site branch blog post), `find` scans on `src/`, `docment/`, `.claude/worktrees/`
