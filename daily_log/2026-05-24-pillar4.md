# Daily Status — 2026-05-24 (Deep Dive, Pillar 4: DX)

- **State:** `submain@22015c4` in sync, no drift, no submain commits, source unchanged. Deliverables on `deep-dive-pillar-4`. Wrote 5 real runnable programs (vec2 math/functional, array-backed stack, enum state machine, 3×3 grid) in `report/pillar4_programs/` and logged friction while writing.
- **Top friction:** (DX1) `let x = expr` is a parse error — type-inferred bindings need two statements; appears in nearly every program. (DX2) enums can't be function param/return types (`Light` ≠ `enum Light`, `enum Light` won't parse) — blocks factoring enum logic into functions; no fixture covers it. (DX3) no string concat (`a+b`) and no `len`.
- **Worst error DX (DX8):** parse errors leak raw internal token names + Rust debug formatting — `ExpectedFound { expected: ['PunctColon','KeywordFnc',...] }`. Every syntax mistake looks like this. Highest-frequency lowest-quality message.
- **Tooling/docs:** no `--help`/`--version` (DX10); the nice `--test` in-language test runner is undocumented (DX11); syntax doc's "Known Gaps" omits the real gaps and defers to reading source as source-of-truth (DX13/14). Array triple-redundant declaration + mandatory full literal (DX4, cross-ref Pillar 1).
- **Delivered:** `report/dx.md` (friction by impact, quick-wins list, doc sections to write, what works). Quick wins: allow `let x=expr`, humanize parse errors, array fill shorthand, `--help`, `len()`. No code changed. **Next: final synthesis (`report/deep_dive_synthesis.md`) combining all four pillars.**
