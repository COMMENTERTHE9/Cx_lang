## What landed today
- 6279d39 Revert "Add arrays, index write, while-in loops, and struct scaffolding"
- 029b9ad Add arrays, index write, while-in loops, and struct scaffolding

## Decisions made
- Arrays, index write, while-in loops, and struct scaffolding were added then reverted — indicates the feature set wasn't ready or introduced regressions that need to be resolved before re-landing
- The revert touched frontend (AST, parser, semantics), IR lowering, and runtime, showing these features cut across the full compiler pipeline

## Context for blog
Tone: technical dev log
Audience: developers following Cx language development
Highlight: An ambitious push to add arrays, index writes, while-in loops, and struct scaffolding was reverted — a sign that getting compound types right across the full AST→IR→runtime pipeline requires more careful staging
