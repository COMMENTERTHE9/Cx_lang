---
title: "Cx Dev Log — 2026-04-10"
date: 2026-04-10
description: "Third consecutive idle day with the submain branch now fifteen days unmerged, carrying five commits that resolve four hard blockers."
---

Fifteen days. That is how long five commits have been sitting on `submain`, resolving four hard blockers, with a clean diff against `main` and zero source conflicts visible. No merge happened today. No merge happened yesterday. No merge happened the day before that.

This is the third consecutive day with zero development activity across all branches. No commits by any author, no merges, no uncommitted work. Matrix holds at 78/78. The project is structurally stalled on a single action: merging submain to main.

## The submain gap

The five commits on `submain` have been static since March 28:

1. **Wrapping arithmetic enforcement** (`d93582b`) - switched from saturating to wrapping semantics, including `i128::MIN` edge cases
2. **Phase 10 while loop lowering** (`0fa06a6`) - header/body/exit CFG, loop-carried SSA, backedge handling, three new tests
3. **UTF-8 decision locked** (`09ef2a3`) - strict UTF-8 everywhere, no legacy encoding paths
4. **Semicolon rule** (`e214d15`) - optional everywhere except expression statements
5. **Basic test runner** (`762b188`) - `assert`, `assert_eq`, `--test` mode, tests t77 through t80

The diff: 24 files changed, 367 insertions, 501 deletions. This is real, tested work. It closes the test runner hard blocker, the integer overflow hard blocker, and the semicolon hard blocker. The UTF-8 hard blocker was already annotated as resolved with a submain reference.

None of it is on main.

## Branch backlog

The daily-log branch situation is compounding. Ten unmerged branches have accumulated: `daily-log-2026-03-29`, then `daily-log-2026-04-01` through `daily-log-2026-04-09`. Each branch touches `daily_log/` and some touch `docs/frontend/ROADMAP.md`. The longer these sit, the more likely they produce merge conflicts in the roadmap file, which has already diverged across branches.

This is a minor issue on its own, but it stacks with the submain gap to create a growing surface of merge debt.

## Roadmap state

The roadmap at `docs/frontend/ROADMAP.md` (v4.8, dated March 28) still accurately reflects the state of `main`. Hard blocker checkboxes for the test runner, integer overflow, and semicolons remain unchecked because those items live on submain, not main. Updating the roadmap before the merge lands would be premature and misleading.

## What is actually next

The same list as yesterday, and the day before:

1. **Merge submain to main.** Fifteenth consecutive day this has been predicted and deferred. Five commits, four hard blockers, no source conflicts in the diff. This is the single highest-value action and the primary bottleneck.

2. **Merge the daily-log backlog.** Ten branches. Batch-merging the oldest ones first would reduce the conflict surface for later ones.

3. **Error model (Result\<T\>).** The next hard blocker after submain work merges. Syntax is locked in the spec: `Result<T>`, `Ok`, `Err`, `?` operator. No implementation exists on any branch.

4. **`:=` type inference.** Quality gate, unstarted. Listed as "strongly desired" for 0.1.

Three days of idle predictions and zero followthrough. The submain merge remains the critical path. Everything downstream, including the error model and type inference, is blocked until it lands.
