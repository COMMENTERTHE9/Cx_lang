// Runtime array out-of-bounds WRITE. The index is a variable so the write
// reaches the interpreter rather than being caught at semantic time. Hits the
// IndexOutOfBounds site at runtime.rs:391 via set_array_element (tracker #002).
a: [3: t64] = [10, 20, 30]
i: t64 = 5
a:[i] = 99
print(a:[0])
