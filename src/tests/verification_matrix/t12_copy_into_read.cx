fnc: t64 outer() {
    x: t64 = 10;
    y: t64 = 20;
    z: t64 = 30;

    fnc: t64 inner(t: copy_into(x, y, z)) {
        t.x + t.y + t.z
    }

    result: t64 = inner(copy_into(x, y, z));
    result
}

print(outer());
