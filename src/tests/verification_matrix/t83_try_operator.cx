fnc: Result<t32> parse_positive(x: t32) {
    if x < 0 {
        return Err("negative");
    }
    return Ok(x);
}

fnc: Result<t32> double_positive(x: t32) {
    let val;
    val = parse_positive(x)?
    return Ok(val * 2);
}

print(double_positive(5))
print(double_positive(-1))
